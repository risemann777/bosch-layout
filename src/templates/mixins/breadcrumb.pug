mixin breadcrumb(data)
  - items = data.items
  ol.breadcrumb.page__breadcrumb(itemscope itemtype="http://schema.org/BreadcrumbList" class=data.class_modifier)
    li(itemscope itemtype="http://schema.org/ListItem").breadcrumb__item
      a(href="/" itemprop="item") Главная
      meta(itemprop="position" content="1")
      meta(itemprop="name" content="Главная")
    each item, index in items
      li(itemscope itemtype="http://schema.org/ListItem").breadcrumb__item
        if item.link
          a(href=item.link itemprop="item")= item.name
        else
          span(itemprop="item")= item.name
        meta(itemprop="position" content=index+2)
        meta(itemprop="name" content=item.name)
